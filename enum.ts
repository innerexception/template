export enum UIReducerActions {
    UPDATE = 'updt',
    NEW='nu',
    CONTINUE='cont'
}

export enum Modal {
    NewGame='ng'
}

export enum SceneNames {
    Loading='loading', Main='main', Intro='intro'
}